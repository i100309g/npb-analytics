# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

NPB Analytics — a web application for analyzing Nippon Professional Baseball (NPB) data, built with Next.js.

## Tech Stack

- **Framework**: Next.js (inferred from `.gitignore` containing `.next/`)
- **Environment**: `.env` / `.env.local` for environment variables

## Common Commands

Once dependencies are installed, typical Next.js commands apply:

```bash
npm install          # Install dependencies
npm run dev          # Start development server
npm run build        # Build for production
npm run lint         # Run ESLint
npm test             # Run tests (if configured)
```
